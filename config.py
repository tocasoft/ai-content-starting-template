# The 'Config' class is the base configuration class, which includes common settings
class Config(object):
    # Debug mode is enabled by default in the base configuration class
    DEBUG = True  # Boolean value that enables or disables debug mode
    
    # Testing mode is disabled by default in the base configuration class
    TESTING = False  # Boolean value that enables or disables testing mode

